import 'src/app_tile_grid.dart';
import 'src/builder.dart';
import 'src/catalog.dart';
import 'src/invariant.dart';
import 'src/validation.dart';

void main() {
  build4sh0();
  verify9zz();
  parse4o3l({});
  localizedhed();
  Gallery();
}

class Gallery extends StatelessWidget {
  Widget build(BuildContext context) => AppTileGrid(
        tiles: [
          .new(label: 'one'),
          .new(label: 'two'),
        ],
      );
}

